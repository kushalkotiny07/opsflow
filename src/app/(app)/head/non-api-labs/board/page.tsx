import { ProviderDailyBoard } from "@/components/head/ProviderDailyBoard";

export const metadata = { title: "Provider Board | OpsFlow" };

export default function ProviderBoardPage() {
  return <ProviderDailyBoard />;
}
